# ActiveTask — Nuevo preset "barbie-dark"

## Objetivo
Crear una versión oscura con hot pink Barbie, derivada del gold (fondo oscuro, acentos rosas).

## Estado actual
- **Completado**: Preset `barbie-dark` en `designPresets.ts`
  - Copia del gold con primary `#ff1493`, botón sólido blanco sobre rosa
  - `sectionBg = sectionBgMid = sectionBgAlt = #0a0a0a` (sin alternancia)
  - Tipografía Outfit + Inter

## Archivos modificados
- `src/features/theme/designPresets.ts` — nuevo preset
- `config/*.config.json` — `designPreset` y `validDesignPresets`
- `docs/ActiveTask.md`, `CHANGELOG.md`, `docs/ROADMAP.md`
